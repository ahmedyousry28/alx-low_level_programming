#include "main.h"
#include <string.h>
/**
 * _strlen - Write a function that returns the length of a string.
 *
 *
 * @s: the sting
 *
 * Return: the length of array
 */
int _strlen(char *s)
{
	return (strlen(s));
}
